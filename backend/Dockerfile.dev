FROM node:20-alpine

WORKDIR /app

# Install dependencies as a separate layer for better caching
COPY package*.json ./
RUN npm ci

# Copy source (volume-mounted in dev so changes are hot-reloaded via ts-node-dev)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
